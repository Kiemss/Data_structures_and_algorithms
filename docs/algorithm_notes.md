1. 数组中元素有一个单个元素，其余均成对，找到单个元素：
   遍历数组中元素，result ^= num;，因为按位异或满足结合律，同时相同的数异或后得到0，所以最终result就是单个元素。太酷啦！

2. 重复相关：考虑使用哈希表
    * 重复一次（两个相同）：甚至可以考虑位图。第一次记录，第二次删除（用set）
    * 重复多次，使用map记录次数

3. 盛最多水问题：在数组头尾设置双指针，每次移动短板
    贪心算法证明：如果不移动短板而是移动长板，则新容量一定比原容量小，所以只能移动短板。太酷啦！